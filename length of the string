#include<stdio.h>
int main() 
{
  char s[20], *p;
  int i = 0;
  printf("Enter Any string : ");
  gets(s);
  pt = s;
  while (*p != '\0') 
  {
    i++;
    p++;
  }
  printf("Length of String : %d", i);
  return 0;
}
